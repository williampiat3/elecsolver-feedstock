schema_version: 1

context:
  name: elecsolver
  version: 2.0.0

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://github.com/williampiat3/ElecSolver/archive/refs/tags/${{ version }}.tar.gz
  sha256: b4c5db16d6e08c6e924a261639551f685735c561f3949424de1075ec94df8ff3



build:
  noarch: python
  script: ${{ PYTHON }} -m pip install . --no-deps --ignore-installed -vv
  number: 0

requirements:
  host:
    - setuptools >=60
    - setuptools-scm >=8.0
    - pip
    - python ${{ python_min }}.*
  run:
    - python >=${{ python_min }}
    - numpy
    - networkx
    - scipy

tests:
  - python:
      imports:
        - ElecSolver
  - script:
      - python -m pytest tests
    requirements:
      run:
        - python ${{ python_min }}.*
        - python-mumps
        - pytest
    files:
      source:
        - tests

about:
  homepage: https://github.com/williampiat3/ElecSolver
  summary: Electric system formulation in Python
  license: MIT
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - williampiat3
    - Pimss
